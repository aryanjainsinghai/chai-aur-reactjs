import React from "react";

// context is like global variable
const UserContext = React.createContext(); // Har ek context ek provider hota 

export default UserContext;

// create provider to provide data